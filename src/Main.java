
import java.util.Scanner;


public class Main {
    public static  void main(String[] args) {
        Scanner input = new Scanner(System.in);

        //------------------------------------------------------------------------------------------------------------------------------------------
        /*int number = -10;
        String sign = (number >= 0) ? "positivo" : "negativo";
        System.out.println("O número é " + sign);

        double saldo = 25;
        double valorSolicitado = 10;

        //String saldoAtual = (saldo > valorSolicitado) ? valueOf(saldo - valorSolicitado) ? "Valor insuficiente" : "Valor Retirado com Sucesso";

        String saldoAtual = (saldo > valorSolicitado) ? String.valueOf(saldo - valorSolicitado) : "Valor insuficiente";
        System.out.println("o seu saldo Atual : R$" + saldoAtual);
*/
       /* for (int carneirinhos = 1; carneirinhos <= 20; carneirinhos++){
            System.out.println(carneirinhos + "º Carnerinho");
        }
    System.out.println("Joãozinho dormiu! ");

    */
        //------------------------------------------------------------------------------------------------------------------------------------------
        /*String alunos[] = { "FELIPE", "JONAS","JULIA","MARCOS"};

        for (int x = 0; x< alunos.length;x++){
            System.out.println("O aluno no indice x= " + x + " é " + alunos[x]);
        }
                            ||

        for(String aluno : alunos){
            System.out.println("O nome do aluno é : "+ aluno);
        }*/

        //------------------------------------------------------------------------------------------------------------------------------------------
        //contuine ou break
        //break interrompe a operação e o continue interrompe a interação;

      /*  for(int i =0; i <= 5; i++){
            if(i == 3)
                continue;
            System.out.println(i);
        }
    */
        //------------------------------------------------------------------------------------------------------------------------------------------
    }
}